import { StyleSheet } from "react-native";

export default StyleSheet.create({
  header: {
    justifyContent: "center",
    textAlign: "center",
    fontSize: 40,
    padding: 15
  },
  container: {
    flex: 1,
    marginBottom: "50%"
  }
});
